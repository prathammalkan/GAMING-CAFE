{
  "name": "gamezone",
  "version": "1.0.0",
  "scripts": {
    "build": "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate && python manage.py ensure_superuser && python manage.py seed_gamezone --if-empty"
  },
  "dependencies": {
    "sqlite3": "^6.0.1"
  }
}
